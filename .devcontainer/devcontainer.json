{
    "name": "JIT Compiler workshop",
    // "build": {
    //   "dockerfile": "implementation/JAVA/Dockerfile"
    // },
    "customizations": {
      "vscode": {
        "extensions": [
          "Gruntfuggly.todo-tree"
        ]
      }
  }
  // // Command to run after the container is created
  // "postCreateCommand": "npm install",

  // // Optional VS Code settings inside the container
  // "settings": {
  //   "terminal.integrated.shell.linux": "/bin/bash"
  // },

  // // Forward any ports from the container to the host machine
  // "forwardPorts": [3000]
}
